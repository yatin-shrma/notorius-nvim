return {
    'numToStr/Comment.nvim',
    config = function ()
        local configs = require("Comment")
        configs.setup({
        })
    end,
    --opts = {
    --   -- add any options here
    --}
}
